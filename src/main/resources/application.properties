# DATABASE CONFIG
db.driver=org.h2.Driver
db.url=jdbc:h2:mem:urlaubsverwaltung;DB_CLOSE_DELAY=-1
db.username=sa
db.password=


# LDAP / ACTIVE DIRECTORY ATTRIBUTES
# Attribute that identifies a user by unique username within LDAP / Active Directory
# Recommended attribute for LDAP: `uid`
# Recommended attribute for Active Directory: `cn` or `dn`
security.identifier=uid
security.firstName=givenName
security.lastName=sn
security.mailAddress=mail


# LDAP CONFIG
security.ldap.url=ldap://ldap.localdomain
security.ldap.base=dc=mydomain,dc=com
# Authentication
security.ldap.userSearchBase=ou=accounts
security.ldap.userSearchFilter=(uid={0})
# Data sync
security.ldap.sync.userSearchBase=ou=people,ou=accounts
security.ldap.sync.userDn=uid=username,ou=other,ou=accounts,dc=mydomain,dc=com
security.ldap.sync.password=password

# ACTIVE DIRECTORY CONFIG
security.activeDirectory.domain=mydomain.com
security.activeDirectory.url=ldap://adserver.mydomain.com/
# Data sync
security.activeDirectory.sync.userSearchBase=dc=mydomain,dc=com
security.activeDirectory.sync.userDn=cn=Administrator,cn=users,dc=mydomain,dc=com
security.activeDirectory.sync.password=password


# URL OF APP - needed e.g. for links in emails
application.url=http://localhost:8080/urlaubsverwaltung/


# VERSION OF APPLICATION - needed for API
application.version=${project.version}
