[# th:if="${not sickNote.startDate.equals(sickNote.endDate)}"][(${#temporals.format(sickNote.startDate, 'dd.MM.yyyy')})] [(#{until})] [(${#temporals.format(sickNote.endDate, 'dd.MM.yyyy')})], [(#{__${sickNote.dayLength.name}__})][/][# th:if="${sickNote.startDate.equals(sickNote.endDate)}"][(${#temporals.format(sickNote.startDate, 'dd.MM.yyyy')})], [(#{__${sickNote.dayLength.name}__})][/]