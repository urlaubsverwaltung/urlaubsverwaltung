[# th:if="${not application.startDate.equals(application.endDate)}"][(${#temporals.format(application.startDate, 'dd.MM.yyyy')})] [(#{until})] [(${#temporals.format(application.endDate, 'dd.MM.yyyy')})], [(#{__${application.dayLength.name}__})][/][# th:if="${application.startDate.equals(application.endDate)}"][(${#temporals.format(application.startDate, 'dd.MM.yyyy')})], [(#{__${application.dayLength.name}__})][/]