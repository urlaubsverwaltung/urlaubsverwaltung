<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                            http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <!-- AUTHENTICATION VIA LDAP -->

    <bean id="personContextMapper" class="org.synyx.urlaubsverwaltung.security.PersonContextMapper">
        <constructor-arg ref="personService"/>
        <constructor-arg ref="mailService"/>
        <constructor-arg value="${security.identifier}"/>
        <constructor-arg value="${security.firstName}"/>
        <constructor-arg value="${security.lastName}"/>
        <constructor-arg value="${security.mailAddress}"/>
    </bean>

    <security:ldap-server id="ldapAuthServer" url="${security.ldap.url}/${security.ldap.base}"/>

    <security:authentication-manager alias="authenticationManager">

        <security:ldap-authentication-provider
                server-ref="ldapAuthServer"
                user-context-mapper-ref="personContextMapper"
                user-search-base="${security.ldap.userSearchBase}"
                user-search-filter="${security.ldap.userSearchFilter}"/>

    </security:authentication-manager>

    <!-- SYNC LDAP DATA -->

    <bean id="ldapTemplate" class="org.springframework.ldap.core.LdapTemplate">
        <constructor-arg ref="contextSource"/>
        <property name="ignorePartialResultException" value="true"/>
        <property name="ignoreNameNotFoundException" value="true"/>
    </bean>

    <bean id="contextSource" class="org.springframework.ldap.core.support.LdapContextSource">
        <property name="url" value="${security.ldap.url}"/>
        <property name="base" value="${security.ldap.sync.userSearchBase},${security.ldap.base}"/>
        <property name="userDn" value="${security.ldap.sync.userDn}"/>
        <property name="password" value="${security.ldap.sync.password}"/>
    </bean>

</beans>
