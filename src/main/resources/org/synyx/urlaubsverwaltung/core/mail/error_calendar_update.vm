Der Termin (${eventId}) für folgende Abwesenheit konnte nicht aktualisiert werden:
${absence.person.niceName}
${absence.startDate} - ${absence.endDate}

Beim Synchronisieren mit dem Kalender '${calendar}' ist ein Fehler aufgetreten:

${exception}
