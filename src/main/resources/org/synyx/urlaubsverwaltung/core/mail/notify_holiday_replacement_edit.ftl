Hallo ${application.holidayReplacement.niceName},

der Antrag von ${application.person.niceName}, bei dem du als Vertreter eingetragen wurdest, wurde editiert.

Der Zeitraum bezieht sich auf ${application.startDate.format("dd.MM.yyyy")} bis ${application.endDate.format("dd.MM.yyyy")}, ${dayLength},

