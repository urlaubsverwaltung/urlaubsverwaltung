# NOTE: These file contains only overrides for the default `application.properties` in environment `test`

# DATABASE CONFIG
db.driver=com.mysql.jdbc.Driver
db.url=${UV_DB_URL:jdbc:mysql://127.0.0.1:3306/urlaubsverwaltung}
db.username=${UV_DB_USER:root}
db.pwd=${UV_DB_PASSWORD:}
