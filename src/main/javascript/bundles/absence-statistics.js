// Placeholder entry point so the asset pipeline resolves `asset:src="absence_statistics.js"` in
// absences/absence_statistics.html (Task 07). The actual chart wiring (window.absenceStatistics ->
// ApexCharts) is Task 08's job.
(function absenceStatisticsPlaceholder() {
  // intentionally empty
})();
